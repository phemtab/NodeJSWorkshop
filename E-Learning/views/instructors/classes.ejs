<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instructor</title>
</head>
<body>
    <%- include('../layout') %>
    <div class="container">

    <!-- ส่งค่ามาจาก Model แล้ววนลูปค่า -->
    <h2 align="center">Hello : <%= instructor.fname %> </h2>
    <h3>My class</h3>
    <table class="table">
        <thead>
          <tr>
            <th scope="col">ClassID</th>
            <th scope="col">ClassName</th>
            <th scope="col">View Lession</th>
            <th scope="col">Add Lession</th>
          </tr>
        </thead>
        <tbody>
          <!-- เน้นเฉพาะก้อน Object ใน classes -->
          <% if(instructor.classes) { %>
            <% for (var result of instructor.classes ){ %>
                <tr>
                    <td><%= result.class_id %></td>
                    <td><%= result.class_title %></td>
                    <td><a href="/classes/<%= result.class_id %>/lesson" class="btn btn-success">View Lession</a></td>
                    <td><a href="/instructors/classes/<%= result.class_id %>/lesson/new" class="btn btn-primary">Add Lesson</a></td>

                </tr>
            <% } %>
          <% } %>

        </tbody>
      </table>

    <br>
    <h3>Add Class</h3>
    <form class="" action="/classes/register" method="post">

            <div class="form-group">
                <label>รหัสวิชา</label>
                <input type="text" name="class_id" value="" class="form-control">
            </div>

            <div class="form-group">
                <label>ชื่อวิชา</label>
                <input type="text" name="class_name" value="" class="form-control">
            </div>
            
            <div class="form-group">
                <label>ภาพปก</label>
                <input type="text" name="img_url" value="" class="form-control"></ร>
            </div>

            <div class="form-group">
              <label>คำอธิบาย</label>
              <textarea type="text" name="description" rows="8" cols="80" class="form-control"></textarea>
          </div>

            <input type="hidden" name="instructor" value="<%= instructor.fname %>">
            <input type="submit" name="" value="สร้างรายวิชา" class="btn btn-primary">

     </form>
    </div>
</body>
</html>